(ns sicp.core
  (:require [sicp.frame-painter :refer [frame-painter-main]])
  (:require [sicp.e2_51 :refer [e2_51-main]])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (e2_51-main))
